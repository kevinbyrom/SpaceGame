using System;
using System.Collections.Generic;
using System.Text;
using Microsoft.Xna.Framework;
using Microsoft.Xna.Framework.Graphics;
using XNAGameLib2D;


namespace WindowsGame1
{
    /*\public class SmokeParticle : Particle
    {
        public SmokeParticle(IWorld world) : base(world)
        {
                            
            // Load the graphics

            Load("smoke.xml");


            // Set the max time to a random value

            Random rnd = new Random();
            this.m_MaxLifeMsecs = 10000 + rnd.Next(1000);


            // Setup the modifiers

            float speed = 50 + rnd.Next(100);

            m_ScreenModifiers.Add(new TintModifier(new Vector4(1,1,1,.9f), new Vector4(.5f,.5f,.5f,0), m_MaxLifeMsecs / 4));
            m_WorldModifiers.Add(new PositionModifier(new Vector3(0,-1,0), speed, m_MaxLifeMsecs));
            m_WorldModifiers.Add(new ScaleModifier(2f, 1, m_MaxLifeMsecs));
 
        }

    }*/
}
